import { ChildPanelHostDirective } from './child-panel-host.directive';

describe('ChildPanelHostDirective', () => {
  it('should create an instance', () => {
    const directive = new ChildPanelHostDirective();
    expect(directive).toBeTruthy();
  });
});
